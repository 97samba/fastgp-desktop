import { Typography } from "@mui/material";
import { Box } from "@mui/system";
import React from "react";

const NotFound = () => {
  return (
    <Box>
      <Typography>Page not found, danga rerr</Typography>
    </Box>
  );
};

export default NotFound;
